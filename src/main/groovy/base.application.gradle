plugins {
    id "application"
    id "configuration.include"
}

jar {
    manifest {
        attributes(
                "Main-Class": application.mainClass,
                "Multi-Release": "true",
                "Enable-Native-Access": "ALL-UNNAMED"
        )
    }
}
